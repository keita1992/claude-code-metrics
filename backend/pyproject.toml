[project]
name = "claude-code-dashboard-backend"
version = "0.1.0"
description = "Claude Code usage dashboard backend"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "polars>=1.0.0",
    "orjson>=3.10.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
]
